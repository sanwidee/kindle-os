<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"
      xmlns:opds="http://opds-spec.org/2010/catalog"
      xmlns:dc="http://purl.org/dc/terms/">
  <id>{{ feed_id }}</id>
  <title>{{ feed_title }}</title>
  <updated>{{ updated }}</updated>
  <author><name>kindle-os</name></author>
{%- for link in links %}
  <link rel="{{ link.rel }}" href="{{ link.href }}" type="{{ link.type }}"{% if link.title %} title="{{ link.title }}"{% endif %}/>
{%- endfor %}
{%- for entry in entries %}
  <entry>
    <id>{{ entry.id }}</id>
    <title>{{ entry.title }}</title>
    <updated>{{ entry.updated }}</updated>
{%- if entry.summary %}
    <summary type="text">{{ entry.summary }}</summary>
{%- endif %}
{%- for link in entry.links %}
    <link rel="{{ link.rel }}" href="{{ link.href }}" type="{{ link.type }}"/>
{%- endfor %}
  </entry>
{%- endfor %}
</feed>
